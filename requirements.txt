Flask>=2.0
pandas>=1.0
opencv-python
deepface
fer

# Note: `deepface` pulls several heavy dependencies (tensorflow/torch). If you want
# a lightweight prototype, remove `deepface` and the app will fall back to a
# safe 'unknown' detection. Install with: pip install -r requirements.txt
